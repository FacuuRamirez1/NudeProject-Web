export type StoreType = {
    id: number,
    name: string,
    location: string,
    hour1: string,
    hour2: string,
    hour3: string,
    image: image
};

export type image = {
    id: number,
    url: string,
};